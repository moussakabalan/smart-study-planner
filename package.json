{
  "name": "smart-study-planner",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "db:init": "node database/init.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
